# AWS MCP Server — locked dependencies
# Python >= 3.11 required

# MCP framework (includes Starlette, anyio, pydantic-settings)
mcp[cli]>=1.9.0,<2.0.0

# AWS SDK
boto3>=1.38.0,<2.0.0
botocore>=1.38.0,<2.0.0

# Data validation (MCP pulls this in, but pin explicitly for reproducibility)
pydantic>=2.10.0,<3.0.0
